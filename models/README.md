# Local GGUF model assets

Place the local embedding model binary here when you want the app to use a repo-local fallback copy instead of downloading from Hugging Face at runtime.

Expected file name:
- nomic-embed-text-v1.5-q8_0.gguf

This directory is tracked with Git LFS for large model files.
