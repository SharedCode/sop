package fs

import "testing"

// TestReplicationRegistryCloseError (skipped) placeholder; inducing rm.close() error requires instrumentation not present.
func TestReplicationRegistryCloseError(t *testing.T) {
	t.Skip("rm.close() error injection not feasible without code hooks")
}
